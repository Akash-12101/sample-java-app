version: 0.2

phases:
  pre_build:
    commands:
      - echo Logging in to Amazon ECR..
      - aws --version
      - IMAGE_NAME=757367946571.dkr.ecr.ap-south-1.amazonaws.com/java-container-repo
      - IMAGE_TAG=latest
      - aws ecr get-login-password --region ap-south-1 | docker login --username AWS --password-stdin 757367946571.dkr.ecr.ap-south-1.amazonaws.com

  build:
    commands:
      - echo Build started on `date`
      - echo Running Maven build...
      - mvn clean install
      - echo Building the Docker image...
      - echo Building the Docker image...
      - docker build -t $REPOSITORY_URI:$IMAGE_TAG .

  post_build:
    commands:
      - echo Pushing the Docker image to ECR...
      - docker push $IMAGE_NAME:$IMAGE_TAG
      - echo Creating imagedefinitions.json...
      - DOCKER_CONTAINER_NAME=app-java
      - printf '[{"name":"%s","imageUri":"%s"}]' "$DOCKER_CONTAINER_NAME" "$IMAGE_NAME:$IMAGE_TAG" > imagedefinitions.json
      - echo $DOCKER_CONTAINER_NAME
      - cat imagedefinitions.json

artifacts:
  files:
    - imagedefinitions.json